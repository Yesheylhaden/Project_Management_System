import React from "react";
import ReactDOM from "react-dom/client";
import App from "./DD_App";
import "./DD_index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
