import React from "react";
import ReactDOM from "react-dom/client";
import App from "./FD_App";
import "./FD_index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
